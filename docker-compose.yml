version: "3.9"

services:

backend:
build:
context: ./backend
container_name: kaufeedback-backend
ports:
- "8080:8080"
env_file:
- .env

frontend:
build:
context: ./frontend
container_name: kaufeedback-frontend
ports:
- "80:80"
depends_on:
- backend
